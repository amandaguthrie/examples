---
import {Code} from 'astro-expressive-code/components';
interface Props {
  content: string;
  language?: string;
  title?: string;
  frame?: string;
}

const {content, language, title, frame} = Astro.props;
---

<Code language={language} code={content} title={title} />
